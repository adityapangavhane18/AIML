from package.maths import *

## calling the file inside subpackage
from package.subpackage.mult import mul

## calling the file inside the packge 
print(addition(2,4))
print(subtraction(9,2))
print(mul(5,4))